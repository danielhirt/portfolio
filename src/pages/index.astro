---
import SplashLayout from '../layouts/SplashLayout.astro';
import { SITE_TITLE, SITE_DESCRIPTION } from '../consts';
---

<SplashLayout title={SITE_TITLE} description={SITE_DESCRIPTION}>
	<div class="tracer-box splash-card relative max-w-lg w-full mx-4 bg-bg-dark/60 backdrop-blur-md border border-gray-light/10 px-8 py-10 md:px-10 md:py-12 text-center">
		<div class="text-center mb-6">
			<h1 class="text-4xl md:text-5xl font-bold text-text-dark mb-2">
				Daniel Hirt
			</h1>
			<p class="flex items-center justify-center gap-1.5 text-text-dark-muted">
				<svg class="w-4 h-4 shrink-0" fill="none" stroke="currentColor" viewBox="0 0 24 24" stroke-width="2">
					<path stroke-linecap="round" stroke-linejoin="round" d="M15 10.5a3 3 0 1 1-6 0 3 3 0 0 1 6 0Z" />
					<path stroke-linecap="round" stroke-linejoin="round" d="M19.5 10.5c0 7.142-7.5 11.25-7.5 11.25S4.5 17.642 4.5 10.5a7.5 7.5 0 1 1 15 0Z" />
				</svg>
				Washington, DC
			</p>
			<p class="text-text-dark-muted mt-1">
				Engineering @
				<a href="https://appian.com/" target="_blank" rel="noopener noreferrer" class="text-accent-light hover:text-white transition-colors">Appian</a>
			</p>
		</div>

		<div class="divider my-6" aria-hidden="true"><span>♟</span></div>

		<p class="text-text-dark-muted leading-relaxed text-sm">
			6+ years building with <span class="text-accent-light">Java</span>, <span class="text-accent-light">Go</span>, <span class="text-accent-light">databases</span>, and <span class="text-accent-light">distributed systems</span>, focused on performance and correctness. Chess nerd, homelabber, Arch Linux (btw) enthusiast, lifelong learner.
		</p>

		<div class="divider my-6" aria-hidden="true"><span>♚</span></div>

		<div class="space-y-2 text-sm text-text-dark-muted">
			<p>
				GitHub: <a href="https://github.com/danielhirt" target="_blank" rel="noopener noreferrer" class="text-accent-light hover:text-white transition-colors font-mono">@danielhirt</a>
			</p>
			<p>
				LinkedIn: <a href="https://www.linkedin.com/in/dchirt/" target="_blank" rel="noopener noreferrer" class="text-accent-light hover:text-white transition-colors font-mono">@dchirt</a>
			</p>
			<p>
				Email: <button id="copy-email" type="button" class="text-accent-light hover:text-white transition-colors cursor-pointer font-mono">danielchirt16 [at] gmail (dot) com</button>
			</p>
		</div>

		<div class="divider my-6" aria-hidden="true"><span>♞</span></div>

		<nav class="flex flex-wrap justify-center gap-4 text-sm font-mono">
			<a href={`${import.meta.env.BASE_URL}projects`} class="text-accent-light hover:text-white transition-colors">[projects]</a>
			<a href={`${import.meta.env.BASE_URL}experience`} class="text-accent-light hover:text-white transition-colors">[experience]</a>
			<a href={`${import.meta.env.BASE_URL}blog`} class="text-accent-light hover:text-white transition-colors">[blog]</a>
			<a href={`${import.meta.env.BASE_URL}chess`} class="text-accent-light hover:text-white transition-colors">[chess]</a>
		</nav>
	</div>

	<script>
		document.getElementById('copy-email')?.addEventListener('click', async () => {
			await navigator.clipboard.writeText('danielchirt16@gmail.com');
			const btn = document.getElementById('copy-email');
			if (btn) {
				const original = btn.textContent;
				btn.textContent = 'Copied!';
				setTimeout(() => { btn.textContent = original; }, 2000);
			}
		});
	</script>

	<style>
		.divider {
			display: flex;
			align-items: center;
			gap: 12px;
		}
		.divider::before,
		.divider::after {
			content: '';
			flex: 1;
			height: 1px;
			background: var(--color-gray-dark);
		}
		.divider span {
			color: var(--color-accent-light);
			font-size: 20px;
			line-height: 1;
		}
	</style>
</SplashLayout>
